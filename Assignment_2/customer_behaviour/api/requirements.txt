# Serving-only dependencies for the API Docker image (no notebook / EDA libs).
# The full notebook + API environment is ../requirements.txt.
numpy>=2.0
pandas>=2.2
scikit-learn==1.9.0        # must match the version that pickled model_pipeline.joblib
scipy>=1.13
joblib>=1.3
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
