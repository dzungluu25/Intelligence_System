# Diabetes application — Python dependencies (notebook + API).
# The web client is React + Vite: see web/package.json (Node 18+), not this file.
# Tested on Python 3.14 (Anaconda). Pin loosely; exact versions used are recorded in
# the notebook's section 0 and in api/README.md.

# --- core ML / data ---
numpy>=2.0
pandas>=2.2
scikit-learn>=1.5
scipy>=1.13
joblib>=1.3

# --- notebook / EDA ---
matplotlib>=3.8
jupyter>=1.0
nbconvert>=7.0

# --- explainability ---
shap>=0.46

# --- API ---
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
python-multipart>=0.0.9

# --- misc ---
requests>=2.31          # API smoke tests / scripts

# --- legacy Streamlit client (web/legacy_streamlit/, optional) ---
# streamlit>=1.36
