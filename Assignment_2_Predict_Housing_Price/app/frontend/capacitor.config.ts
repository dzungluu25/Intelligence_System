import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.intelligent.system.housing',
  appName: 'Housing Predictor',
  webDir: 'dist'
};

export default config;
